# 卒業制作 計画書

令和◯◯年度 卒業制作 ／ Network Security section

> このテンプレートをコピーして、各項目を埋めてください。各項目の「記入例」は、あくまで書き方の見本です。
> 空欄のまま埋められない項目は、「これから調べること」として印を付けておきましょう。

## 基本情報

- グループ名:
- メンバー（氏名・学籍番号）:
  - （リーダー）
  -
  -
- 制作タイトル:
  - 記入例: 予算で探せる 近隣グルメサイトの作成

## 制作概要

（何を作るのかを、2〜3行で簡潔に）

記入例: 学校の近くで、予算内で入れる飲食店を探せるWebサイトを制作する。利用者は予算を入力すると、条件に合う店の一覧を見られる。

## 目的（なぜ作るのか）

（何を解決したいのか・何を確かめたいのかを書く。「〜の使い方をまとめる」で終わらないこと）

記入例: 「予算で店を絞り込める」サイトが見当たらず、店探しに手間がかかる。この不便を、予算での絞り込み機能で解決する。

## 完成イメージ

（誰が・どの端末で・どのように使うか）

記入例: 利用者が自分のスマホやPCのブラウザから開き、予算を入れて店を探す。インターネットに公開する。

## 公開方法

（手元でのデモか、常時公開か。使う手段を具体的に）

記入例: さくらのVPS上にApacheを立てて常時公開する。発表前の動作確認は ngrok でも行う。

## 使用する技術・ツール

- プログラミング言語:
- フレームワーク・ライブラリ:
- データベース:
- サーバ・実行環境:

記入例: 言語 = PHP／フレームワーク = なし／データベース = MySQL／サーバ = Apache（Ubuntu, VPS）

## 開発の進め方

- コードを書くエディタ:
- 開発する場所（自分のPC／Remote-SSH でサーバ上）:
- コードの管理:
- 動作の確認方法:

記入例: VSCode + Remote-SSH で VPS 上を直接編集。コードは GitHub で管理。`http://<VPSのIP>/` をブラウザで開いて確認する。

## 作る機能（機能要件）

（作りたい機能を箇条書きで）

- 記入例: 店の一覧表示
- 記入例: 予算による絞り込み
- 記入例: 店の詳細表示
- 記入例: （管理者）店の登録・編集

## 品質の目標（非機能要件）

（速度・使いやすさ・安全性など、機能以外に目指す品質）

- 記入例: 検索結果は2秒以内に表示する
- 記入例: スマホの画面でも見やすくする

## 設計

### 基本設計（全体の構成）

（使うコンポーネントと、その関係。可能なら構成図も）

記入例: ブラウザ → Apache（表示）→ PHP（処理）→ MySQL（店データ）の流れ。

### 詳細設計

- データベースのテーブル:
  - 記入例: `shops`（id、店名、予算、住所、電話番号）
- Webサーバー（Apache）の設定:
  - 公開ポート（80 / 443）:
  - HTTPS にするか:
  - DocumentRoot（ファイルを置く場所）:
  - 編集する設定ファイル:
  - 記入例: ポート = 80／HTTPS = まずはしない／DocumentRoot = `/var/www/html`／設定ファイル = `/etc/apache2/sites-available/000-default.conf`
- ネットワーク構成（ネットワークを使う場合）:
  - 記入例: IPアドレス、サブネット、ルーターの設定など

## 必要環境（ハードウェア・ソフトウェア）

（制作・動作に必要な端末やソフト）

記入例: 開発用PC、さくらのVPS（Ubuntu）、Apache、MySQL、PHP
